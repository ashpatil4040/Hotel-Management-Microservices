server:
  port: 8089

spring:
  application:
    name: CONFIG-SERVER


  cloud:
    config:
      server:
        git:
          uri: https://github.com/ashpatil4040/microservice-config
          clone-on-start: true
                              